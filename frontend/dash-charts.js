/* signal-charts.js · optional progressive-enhancement stub.
   The dashboard renders every chart inline (hand-rolled SVG in index.html) and
   is complete without this file. init() calls window.StoicCharts.enhance(M)
   only behind an `if (window.StoicCharts)` guard, so a no-op keeps that hook
   satisfied without altering any rendered output. */
(function () { "use strict"; window.StoicCharts = { enhance: function () {} }; })();
