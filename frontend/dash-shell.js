/* signal-shell.js · optional progressive-enhancement stub.
   Shell chrome (topbar, footer, toggles) is authored inline in index.html and
   is complete without this file. init() calls window.StoicShell.enhance(M) only
   behind an `if (window.StoicShell)` guard, so a no-op keeps that hook satisfied
   without altering any rendered output. */
(function () { "use strict"; window.StoicShell = { enhance: function () {} }; })();
